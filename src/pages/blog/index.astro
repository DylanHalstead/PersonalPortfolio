---
import { getCollection } from "astro:content";

const blogPosts = (await getCollection("blog")).sort((a, b) => {
  return b.data.sortOrder.valueOf() - a.data.sortOrder.valueOf();
});
// blogPosts.forEach((post) => {console.log(post.id.valueOf())})
---